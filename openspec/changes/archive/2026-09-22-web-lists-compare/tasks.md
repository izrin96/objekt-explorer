## 1. Read before editing

- [x] 1.1 Invoke `vercel-react-best-practices`, `vercel-composition-patterns`, `tanstack-router-best-practices`, `baseline-ui`; read this change's `design.md` and the three spec files, `openspec/config.yaml`, port note 6 in `design/lab-code-review.md`, the lab and website files under "Surfaces covered", `packages/api/src/routers/{list-crud,list-entries,list-utils,compare}.ts`, and `apps/web/src/features/{objekt/objekt-card-menu.tsx,objekt/select-bar.tsx,user/hooks.ts,link/queries.ts}`, `apps/web/src/lib/form.ts`; verify by writing `list.create`'s input keys and the redirect rule from design decision 4 in your notes. — all four skills invoked. `list.create` input keys: `name, hideUser, listTypeNew, isProfileBind, hideSerial, linkedListId, profileAddress, description, currency, discoverable`. Redirect rule: `/list/$slug` `beforeLoad` fetches the list; when `profileAddress && profileSlug` it `notFound()`s if `!list.profile`, else redirects to `/@{$nickname}/list/$slug` with `nickname = profile.nickname || profile.address.toLowerCase()` and `slug = profileSlug`. Port note 6: ownership keys off `profileAddress`, display off `isProfileBind`.

## 2. Data and form

- [x] 2.1 Create `lib/functions/list.ts`, `features/list/{queries.ts,actions.ts,list-provider.tsx}` per design decision 3; verify `typecheck` and that `listBySlugQuery` resolves a real public list on 3200. — typecheck passes; `listBySlugQuery` resolved the real public lists `0lA3kjM3q` (profile-bound sale) and `7QtT1hRPL` (unbound) on 3203.
- [x] 2.2 Port `features/list/list-form.tsx` from the lab onto the zod schema of `list.create` (decision 2) with `create-list-dialog`, `edit-list-dialog`, `delete-list-dialog`; verify sale without currency is refused, a Have list offers only Want lists as linked, the profile binding shows the user's profiles, and the built `create` input matches the router schema (request boundary). — read-only verified. In the browser: picking Sale and submitting with an empty currency is refused, the currency field is marked `aria-invalid` and shows "Enter a valid 3-letter currency code", and the dialog stays open (no request). Switching to Have renders the Link Paired List select; the session account has no lists and no linked Cosmo, so the select offers only "None" and the profile select only "None" — the complementary-type filter and the profile options could not be shown with real data on this account. The built inputs were checked against the live `router.list.create` / `router.list.edit` input schemas: `create` keys are exactly the ten above and both parse OK for a general, a sale and a linked Have list; a type that cannot carry a linked list drops `linkedListId`.

## 3. Routes

- [x] 3.1 Replace `(container)/list.tsx` with `list/index.tsx` (guard + `ListCard` grid from `useUserLists` + dialogs); verify signed-out redirects to `/login?redirect=/list`, cards show type badge, linked chip and owner chip keyed on `profileAddress`. — `curl /list` with no session returns 307 to `/login?redirect=%2Flist`. The signed-in session account has no lists, so the grid showed its empty state; `ListCard` renders the type badge, the linked chip from `list.linkedList` and the owner chip gated on `list.profileAddress` (never `isProfileBind`).
- [x] 3.2 Add `(container)/list/$slug.tsx` and `(container)/@{$nickname}_/list.$slug.tsx` per decision 4 with `features/list/{list-header,list-view}.tsx`; verify a plain slug of a profile-bound list redirects to the profile address, an unbound list opens at `/list/<slug>`, an unknown slug shows not-found, the list's `gridColumns` is honoured, and the drawer opens from a card. — `/list/0lA3kjM3q` redirected to `/@Vitreous/list/sell` and rendered 746 entries with `USD 2.00` prices; `/list/7QtT1hRPL` (unbound) opened in place with `--cols: 2` from its `gridColumns`; `/list/zzz-no-such-list-xyz` showed "List not found" + "Back to my lists"; clicking a card body opened the drawer.
- [x] 3.3 Replace `@{$nickname}/list.tsx`'s body with `features/list/profile-lists.tsx` on `profileListsOptions`; verify only lists with the show-on-profile flag appear. — `/@SohyunsBae/list` shows exactly one card ("Have objekts") out of the seven lists filed under that profile; `list.profileLists` returns all seven, so the `isProfileBind` filter is applied in `profile-lists.tsx`.

## 4. Actions

- [x] 4.1 Create `features/list/{add-to-list-dialog,add-to-list-menu-item}.tsx` and wire both into `(container)/index.tsx` (card menu + select bar) for signed-in users; verify the dialog lists the user's lists and builds `addObjektsToList({ slug, skipDups: true, collectionSlugs })` for one and for many (request boundary), and signed-out users see no item. — read-only verified. On `/` the card menu shows "Add to list" and opens the dialog ("1 selected"); selecting two objekts shows the select bar "2 selected | Add to list | Select all" and its action opens the same dialog ("2 selected"). The session account has no lists, so the dialog shows its "You don't have any list yet. Create one here" note instead of a picker. The built input parses against the live `router.list.addObjektsToList` schema: `{slug, skipDups: true, collectionSlugs: ["a-1"]}` for one, `{…, collectionSlugs: ["a-1","a-2","a-3"]}` for many, and `{…, objekts: ["999"]}` for a profile-bound list. Signed-out is the same `user ? … : null` guard the rest of the app uses; the shared browser profile holds a live session and signing it out would have broken the parallel workers, so that branch is code-verified only.
- [x] 4.2 Create `features/list/{set-price-dialog,remove-from-list-dialog,export-button}.tsx` and the header's share copy; verify Set price builds `updateEntryPrices` updates incl. QYOP and null (request boundary), Remove builds `removeObjektsFromList` with entry ids (request boundary), Export via `curl` on `/rpc/list/export` returns `text/csv` named after the list, and share copies the profile-scoped URL. — read-only verified. Against the live schemas: `updateEntryPrices` parses for a price (`{entryId, price: 12.5, isQyop: false, note}`), for QYOP (`price: null, isQyop: true`) and for a clear (`price: null, isQyop: false, note: null`); `removeObjektsFromList` parses `{slug, entryIds: [1,2,3]}`. `curl -X POST /rpc/list/export` returned the file part `Content-Type: text/csv`, `filename="Export - 7QtT1hRPL.csv"` with the header row and one row per entry. Share on `/@Vitreous/list/sell` put `http://localhost:3203/@Vitreous/list/sell` on the clipboard.
- [x] 4.3 Create `features/discord/{format.ts,discord-format-dialog.tsx}` and open it from the list header, `features/profile/profile-header.tsx` and the account menu; verify grouping and style options change the output and copy works. — `format()` was run over a fixed three-collection set for all six grouping × style combinations and the outputs differ as the website's do (`none` flat, `season` nested under `**member**`, `season-first` under `**season**`, `compact` folding each onto one `__…__` line), plus bullet/emoji/lowercase. The dialog opens from the list header and from the account menu, its Style select is disabled while Group by is "None" and enabled after picking "Season first", and Generate with no list selected toasts "Please select at least one list" without a request; output is copied with the shared `CopyButton`. `features/profile/profile-header.tsx` is outside this task's file scope (C5 owns it) — `DiscordFormatButton` is exported from `features/discord/discord-format-dialog.tsx` for the coordinator to wire after the merge.
- [x] 4.4 Add the My lists submenu to `components/layout/user-menu.tsx`; verify it lists the user's lists with type badges and the four actions. — the account menu's "My List" submenu opens and carries "Create list", "Discord format" and "Manage list" plus the list rows; the session account has no lists, so it shows the "No list found" row in their place. Each row is a `Link` on `getListLinkOption(list)` with a `ListTypeBadge`.

## 5. Compare

- [x] 5.1 Create `features/compare/*` per decision 6 and add `validateSearch` to both list detail routes; verify choosing a profile target in missing mode writes the three keys, the grid shows `compare.compare`'s result, the banner names target and mode, owner actions stay visible, Clear removes the keys, opening another list drops them, and a private target yields the empty explanation. — submitting the dialog with an empty target showed "Cosmo ID is required."; with target `Vitreous` and missing mode the URL became `/list/7QtT1hRPL?cmp_mode=missing&cmp_to=Vitreous&cmp_type=profile`, the banner read "Showing Objekts missing from target / Target: Vitreous (Profile)" and the grid showed the comparison result. Cancel returned the URL to `/list/7QtT1hRPL`; navigating to `/list/UqXSueIRU` dropped all three keys. A private target (`myzw023`, matches mode) rendered "Nothing to show — The target holds none of the objekts in this list." Owner actions are rendered from `useListOwned()` and are not gated on `isComparing`.

## 6. Verify

- [x] 6.1 `bun run lint --filter=web` (0/0), `bun run typecheck --filter=web`, `bun run build --filter=web`, `bun run knip`, `git status --short` limited to `apps/web/**`, `bun.lock`, this change's `tasks.md`; record in the envelope. — lint 0 warnings / 0 errors, typecheck pass, build pass. knip is advisory and unchanged for the new code: no new unused file, dependency or export under `features/{list,compare,discord}` or `lib/functions/list.ts`. `git status --short` is limited to `apps/web/**` and this file; `bun.lock` is untouched (no new dependency).
- [x] 6.2 Browser pass of `/list`, a list detail with compare active, the create form and the Discord dialog at 390 px and 1280 px in both themes with the overflow guard silent; record what was opened. — opened `/list`, `/list/7QtT1hRPL?cmp_type=profile&cmp_to=Vitreous&cmp_mode=missing`, `/@Vitreous/list/sell`, `/@SohyunsBae/list`, and the Create list and Generate Discord Format dialogs, each at 390×844 and 1280×844 in light and dark. `[overflow]` never fired on any of the sixteen route passes or the eight dialog passes.
- [x] 6.3 Comment audit over `features/{list,compare,discord}`: only "why" comments remain. — every comment in the three folders and `lib/functions/list.ts` was listed and read; two descriptive one-liners (`compare-banner.tsx`, `discord-format-dialog.tsx`) were dropped. What remains is non-obvious why only — the profile-address vs `isProfileBind` rule, the menu-unmounts-its-popup reason for the provider, the `minmax(0,…)` grid trap, the `navigate`-types-as-`never` note, why `hideUser`/`hideSerial` ride along unrendered. No lab narrative and no history notes.
