## 1. Read before editing

- [x] 1.1 Invoke `vercel-react-best-practices`, `vercel-composition-patterns`, `tanstack-router-best-practices`, `better-accessibility`, `baseline-ui`; read this change's `design.md` and specs, `openspec/config.yaml`, port notes 4–6 in `design/lab-code-review.md`, the lab and website files under "Surfaces covered", and `apps/web/src/features/{objekt/objekt-virtual-grid.tsx,objekt/build-virtual-data.ts,objekt/objekt-card.tsx,filters/search-schema.ts,filters/filter-popover.tsx,link/edit-cosmo-dialog.tsx,link/queries.ts}`; verify by writing the website's `showActions` and `dndEnabled` expressions and `pins.reorderPins` order rule in your notes. — all five skills invoked; `showActions = user && !filters.at`; `dndEnabled = isProfileAuthed && showActions && !isFiltering(filters) && !filters.hidePin`; `reorderPins` takes the pinned tokenIds topmost-first and writes `order = total - i`, so the topmost pin carries the biggest value and `pins.list` returns them ascending by `COALESCE(order, id)`.

## 2. Data

- [x] 2.1 Add `@dnd-kit/{core,sortable,utilities}` and `react-intersection-observer` at the website's versions; `bun install`; verify single versions. — `@dnd-kit/core 6.3.1`, `@dnd-kit/sortable 10.0.0`, `@dnd-kit/utilities 3.2.2`, `react-intersection-observer 11.0.1`, one entry each in `bun.lock`.
- [x] 2.2 Copy `routes/api/objekts/owned-by.$address.ts` and `routes/api/transfers.$address.ts` from the website against `@repo/api`; add `lib/functions/profile.ts` (+ `lib/server/middleware.ts` if needed); create `features/profile/queries.ts` and `use-profile-objekts.ts` per design decisions 3–4; verify on 3200 that owned-by pages by cursor, `?at=` returns the historical set, and a hidden address returns an empty list. — verified on 3202: `?limit=3` then the returned cursor gave the next page; `?at=2025-06-01` returned 2 of the 4 objekts and `?at=2025-09-01` all 4; a `privateProfile` address returned `{"objekts":[]}` and a `hideTransfer` address `{"hide":true,"results":[]}`.

## 3. Layout

- [x] 3.1 Replace `routes/@{$nickname}/route.tsx` and add `index`, `trades`, `progress`, `stats`, `list` (placeholder) routes per decision 6 with `features/profile/{profile-provider,profile-guard,profile-banner,profile-header,profile-stats,profile-tabs}.tsx` from the lab; verify a public profile renders banner, header, four counts and five tabs, a private profile shows only the guard with no objekt requests in the network log, an unknown nickname shows not-found, and the owner sees Edit opening `EditCosmoDialog`. — `/@vziyn` renders both banner copies and the spacer; `/@yatai` renders header, the four counts and the five tabs; `/@JakeyBoi` (private) renders only the guard and the CDP network log carried no objekt, pin or transfer request (only `/api/auth/list-accounts` and `/rpc/status/get`); `/@zzz-not-a-real-nickname-xyz` shows "Page not found"; the owner's Edit Profile button opens `EditCosmoDialog` (owner view reached through a temporary dev-only flag, reverted — the browser session has no linked Cosmo).

## 4. Collection

- [x] 4.1 Extend `features/objekt/build-virtual-data.ts` (pin ordering behind `isProfile`) and `objekt-virtual-grid.tsx` (`onLoadMore` / `hasNextPage` via `InView`); verify home still renders unchanged and a large profile loads a second page on scroll. — home renders its grid unchanged at 390 px and 1280 px; `/@magpieS` went from "8,000 shown … of 8,000+ owned" to "16,000 shown … of 16,000+ owned" when the tail sentinel scrolled into view.
- [x] 4.2 Make the lock filter tri-state in `filters/filter-popover.tsx` and `filter-utils.ts`; verify three activations write `locked=true`, `locked=false`, then remove the key, and the grid follows. — the Select became a cycling control; three activations on `/@shai3005` wrote `?locked=true` (1,891 shown), `?locked=false` (1,422 shown), then no parameter (3,313 shown). `filterObjekts` already honoured `false`, so `filter-utils.ts` needed no change.
- [x] 4.3 Port `features/profile/{profile-toolbar,checkpoint-popover,pinned-shelf,pin-dnd,collection-view,actions}.ts*` per decisions 2, 3, 5 and wire `index.tsx`; verify: pinned shelf first in `pinOrder`; lock marks; per-profile `gridColumns` honoured; visitors see no actions; setting a checkpoint writes `at=`, hides shelf, marks and actions, and survives a filter change; Reset keeps `at`; drag and keyboard reorder produce one `reorderPins` request in the correct token order (request boundary only unless a write is approved); batch pin/lock buttons build the correct `batchPin` / `batchLock` inputs (request boundary). — shelf renders the pins in `pinOrder` and the grid leads with them (`/@hyebamiya`, 131 pins); `?locked=true` on `/@shai3005` puts a lock mark on every visible card; `/@Wasabin` grids at 3 columns and `/@slshxydh` at 4; a signed-in non-owner sees only "Select all" and no card menu; the Snapshot popover wrote `?at=2025-06-01T15:59:59.999Z`, the shelf, marks and select controls all disappeared and the notice appeared, `at` survived adding `member` and `locked` and survived "Reset filter". Read-only verified at the request boundary with the submit intercepted (CDP `Fetch.failRequest`, nothing reached the server): `batchPin → {address, tokenIds:[29253077,28916935,28961171]}`, `batchLock → {address, tokenIds:[29253077,28916935]}`, keyboard drop → exactly one `reorderPins` whose tokenIds matched the post-drop shelf order topmost-first, pointer drag → exactly one `reorderPins` likewise.

## 5. Trades, progress, stats

- [x] 5.1 Create `features/profile/trades/{queries.ts,trades-view.tsx}` on `/api/transfers/$address` and wire `trades.tsx`; verify types filter, paging appends, hidden-transfer profiles show the notice, hidden nicknames show as addresses. — on `/@yatai/trades` the Event control offers All/Mint/Received/Sent/Spin; Mint left only "Received" rows from COSMO and Spin only "Sent" rows to COSMO Spin; scrolling the tail sentinel took the table from 150 to 300 rows; `/@JakeyBoi`-style `hideTransfer` profiles render the "Activity History Private" notice; on `/@WAVsteph/trades` a counterparty with a hidden Cosmo ID renders as `0x3281…613a`.
- [x] 5.2 Create `features/profile/progress/{shape-progress.ts,progress-view.tsx,member-progress-chart.tsx}` and wire `progress.tsx`; verify Welcome and Zero are excluded, a multi-member objekt counts once per selected member, and the chart totals match the panel counts. — `/@yatai/progress?member=Choerry` lists no Welcome or Zero class card; `?member=JiWoo` shows a Unit class card under the `JiWoo *` sections, so a unit objekt is counted under the selected member; the Overall panel (`1.5% · 234 / 15,548`) and the chart are both reduced from the same `memberProgress` rows.
- [x] 5.3 Create `features/profile/stats/stats-view.tsx` and wire `stats.tsx`; verify member and season charts sum to the owned count. — `/@yatai/stats` draws 63 member sectors and 8 season sectors over the same 353-objekt owned set the API returns (`n=353, members=63, seasons=8`), so both pies total the owned count.

## 6. Verify

- [x] 6.1 `bun run lint --filter=web` (0/0), `bun run typecheck --filter=web`, `bun run build --filter=web`, `bun run knip`, `git status --short` limited to `apps/web/**`, `bun.lock`, this change's `tasks.md`; record in the envelope. — lint 0 warnings / 0 errors, typecheck pass, build pass, knip reports nothing new under `apps/web/src/features/profile` or the new routes (its standing `apps/website` findings are unchanged), and `git status --short` touches only `apps/web/**`, `bun.lock` and this file.
- [x] 6.2 Browser pass of a public profile's five tabs and the checkpoint state at 390 px and 1280 px in both themes with the overflow guard silent; record what was opened. — `/@yatai`, `/@yatai/trades`, `/@yatai/progress`, `/@yatai/stats`, `/@yatai/list` and `/@yatai?at=2025-06-01T15:59:59.999Z` at 390×844 and 1280×900 in Light and Dark: no horizontal document scroll and no `dev-overflow-guard` console error on any of them.
- [x] 6.3 Comment audit over `features/profile` and the touched `features/objekt` files: only "why" comments remain. — every remaining comment states a non-obvious why (dnd-kit's touch/long-press split, the drag-end commit rule, the handle's missing `stopPropagation`, the checkpoint draft, the pin-order rule, the `pinFirst` guard, the grid's `key` on column count); the descriptive ones added while porting were removed.
