# Parity review round 2 — shell

Date: 2026-09-22
Commits reviewed: `157b09c7` (both apps, branch `web/main`). Round-1 baseline `340b1222`, four commits back.

| | |
| --- | --- |
| Reference | `apps/website` on `http://localhost:3000` (signed out) |
| Under review | `apps/web` on `http://localhost:3200` (browser profile signed in as the smoke account) |
| Viewports | 1280×900, 1920×1000 (wide check), 390×844 `mobile: true` |
| Themes | web: `Light` / `Dark` through the settings store; website: `localStorage.theme` + `Emulation.setEmulatedMedia` `prefers-color-scheme` |
| Locales | `en`, `ko`, `ja` via `PARAGLIDE_LOCALE` set with CDP `Network.setCookie` on each origin |
| Skills invoked | `better-accessibility` (a11y axis), `web-design-guidelines` (shell UI axis, guidelines fetched fresh from `vercel-labs/web-interface-guidelines`) |

**Browser method.** Three tabs created by me via CDP `Target.createTarget` and driven only by their own target ids — the shared Chrome was never acted on through "the active tab", and all three were closed at the end. A third tab lived in its own `Target.createBrowserContext` so signed-out `web` surfaces could be compared without touching the smoke session. `Emulation.setFocusEmulationEnabled` was needed before CDP keyboard/mouse input reached the background tabs. Console errors were captured with `Page.addScriptToEvaluateOnNewDocument` patching `console.error`/`warn`, `onerror` and `unhandledrejection`.

**Signed-in surfaces.** The root `.env` sets `VITE_SITE_URL=http://localhost:3200`, so Better Auth's `baseURL` resolves the session cookie on `:3200` only — same constraint as round 1. `apps/web`'s signed-in shell (avatar menu, account dialog, My List nav link) was exercised live; `apps/website`'s signed-in shell was read from source (`apps/website/src/components/layout/user-nav.tsx`). No sign-out, no sign-up, no form submit, no `db:*`; every write was to `localStorage` or a locale cookie in my own tabs.

**Skills not run in full.** `better-interface` and `code-review` were not invoked: both fan out across every screen and the whole branch diff, which three other reviewers own. The shell UI axis was covered by `web-design-guidelines` and the a11y axis by `better-accessibility`; the source axis was done by hand against `AGENTS.md`, `openspec/specs/web-shell/spec.md` and `openspec/specs/web-app-shell/spec.md`, with a `file:line` cite on both sides of every row below.

---

## Gaps

| ID | Sev | Surface | Website does (file:line) | Web does (file:line) | Repro |
| --- | --- | --- | --- | --- | --- |
| SH-1 | **P1** | Container build | `apps/website/Dockerfile` (turbo prune → `bun run --filter website build:prod` → compiled `server` binary + `apps/website/dist`, `EXPOSE 3000/tcp`) is what `docker-compose.yml:97` and `.github/workflows/docker-ci.yml:114` both build. | **No `apps/web/Dockerfile` exists** (`ls apps/web/` → no Dockerfile). `apps/web` cannot be built or shipped as a container at all. | `ls apps/web/Dockerfile` → not found; `docker compose build web` has no target. C8 must author it (a copy of the website one with `web`, `apps/web/dist`, and the port fixed per SH-2). |
| SH-2 | **P1** | Runtime port | `apps/website/server.ts:75` — `Number(process.env.PORT ?? 3000)`, matching `docker-compose.yml:94` (`"3000:3000"`) and the healthcheck at `docker-compose.yml:128` (`curl -f http://localhost:3000/api/healthcheck`). | `apps/web/server.ts:75` — `Number(process.env.PORT ?? 3200)`. Swapping the compose service without adding `PORT=3000` (or changing the default) leaves the container listening on 3200, so the healthcheck fails every 5 s and the service restart-loops. | `diff apps/website/server.ts apps/web/server.ts` → only the port constant and its doc comment differ. |
| SH-3 | **P2** | Turbo task graph | `apps/website/turbo.json` declares `build` and `build:prod` outputs `["dist/**", ".tanstack/**"]` and adds `oxlint.config.ts` to `lint` inputs. | **No `apps/web/turbo.json`.** Root `turbo.json` has no `build:prod` task at all and its `build` outputs omit `.tanstack/**`, so `turbo run build --filter=web` caches an incomplete artifact and `turbo run build:prod --filter=web` is not a task. (The Docker build uses bun's `--filter`, so it still works — the loss is caching correctness.) | `ls apps/web/turbo.json` → not found; `cat turbo.json` → no `build:prod`. |
| SH-4 | **P2** | Persisted device settings | Keys `config` (`apps/website/src/hooks/use-config.ts:36` — wide, hideLabel, currency), `columns` (`apps/website/src/hooks/use-breakpoint-column.tsx:30`), `user-searches` (`apps/website/src/hooks/use-user-search-store.ts:30`), `theme` (tanstack-theme-kit default). | Renamed to `web:settings` (`apps/web/src/stores/settings.ts:9`), `web:columns` (`apps/web/src/stores/columns.ts:21`), `web:recent-users` (`apps/web/src/features/user/search-store.ts:26`); theme folded into `web:settings`. Same origin in production, so at cutover **every existing user silently loses theme, wide, hide-label, currency, column count and recent users** and lands back on System/USD/7-columns. | Read `Object.keys(localStorage)` on `:3000` → `['input','theme','columns','config','web:settings']`; the two key sets never overlap. No migration shim anywhere in `apps/web/src`. |
| SH-5 | **P2** | `/@unknown-nickname` | `apps/website/src/routes/@{$nickname}/route.tsx:18,44-51` — a route-local `NotFoundComponent` renders a ghost icon and `m.profile_not_found()` → **"User not found"**. | `apps/web/src/routes/@{$nickname}/route.tsx:5,18` imports the generic `NotFoundComponent` (`apps/web/src/components/router/not-found.tsx:9`), so the page reads **"Page not found / Home"**. The visitor can no longer tell a mistyped path from a Cosmo user who does not exist. `m.profile_not_found` ("User not found") is still shipped in all three catalogues and has no reader. | Open `/@zzzdoesnotexistuser` on both (404 on both). Website `main` text = `"User not found"`; web = `"Page not foundHome"`. |
| SH-6 | **P2** | `bun run knip` gate | `AGENTS.md:66` — knip "becomes a gate once `apps/website` and `intentui/` are deleted". | `bun run knip --workspace apps/web` exits 1 with **131 unused exports**, 130 of them inside `apps/web/src/components/ui/**` (the verbatim cnippet registry, which `AGENTS.md` exempts from review but `knip.json:16-25` does not exempt from knip) plus `checkOverflow` (`apps/web/src/lib/dev-overflow-guard.ts:82`) and 2 unused exported types. The gate fails on the first commit after C8 unless `knip.json` gains an ignore for the registry. | `bun run knip --workspace apps/web`. No unused *files* and no unused *dependencies* — round 1's `banner-field.tsx` is gone. |
| SH-7 | **P2** | Avatar menu → My Cosmo | `apps/website/src/components/layout/user-nav.tsx:201-232` — a submenu listing every linked Cosmo profile as a direct link to `/@{nickname}`, an empty state (`m.nav_no_cosmo_found()`), and a "Manage link" item to `/link`. | `apps/web/src/components/layout/user-menu.tsx:110-113` — one flat item to `/link`. Reaching a linked profile from the menu now costs a page load and a second click. `nav_no_cosmo_found` and `nav_manage_cosmo_link` are both dead in `apps/web/messages/en.json` (neither is on the three-key exemption list). | Open the avatar menu at 1280 signed in: items are Artists, My List, My Cosmo, Account, Setting, About, Sign out — My Cosmo has no submenu arrow. `openspec/specs/web-shell/spec.md` only requires "a My Cosmo item opening `/link`", so the spec was written to the reduced shape. |
| SH-8 | **P2** | Avatar menu header | `apps/website/src/components/layout/user-nav.tsx:92-105` — under the display name, a Discord chip (`user.discord`) and an X chip (`user.twitter`), each with its logo. | `apps/web/src/components/layout/user-menu.tsx:94-99` — avatar + `user.name` only. The linked social identities are no longer visible anywhere in the shell. | Open the avatar menu on `web` signed in; only the name renders. |
| SH-9 | P3 | Header, ≤ `md` | `apps/website/src/components/layout/navbar.tsx:186` names the hamburger `m.nav_toggle_menu()` ("Toggle Menu"); `user-nav.tsx:78` names the avatar `m.nav_open_menu()` ("Open Menu"). Two distinct names. | `apps/web/src/components/layout/mobile-nav.tsx:47` and `apps/web/src/components/layout/user-menu.tsx:83` both use `m.nav_open_menu()`. At 390 the header exposes **two adjacent buttons with the identical accessible name "Open Menu"**. `nav_toggle_menu` is dead in `apps/web/messages/en.json`. | At 390, `[...document.querySelectorAll('header button')].map(b => b.getAttribute('aria-label'))` → web `['Open Menu', 'Changelog', 'Open Menu']`; website `['Toggle Menu', …, 'Open Menu']`. `better-accessibility` → "Accessible names everywhere". |
| SH-10 | P3 | `<meta name="theme-color">` | `apps/website/src/components/shared/theme-provider.tsx:10-18` — a `ThemeWatcher` rewrites the meta to `THEME_COLORS.light` / `.dark` whenever the resolved theme changes. | `apps/web/src/routes/__root.tsx:64-66` emits `THEME_COLORS.dark` once and nothing ever updates it, so in the Light theme the mobile browser chrome and the installed-PWA status bar stay `#09090B` against a `#FBFBFB` page. `THEME_COLORS.light` (`apps/web/src/lib/utils.ts:20`) now has no reader. | Set theme Light on web, reload → `document.querySelector('meta[name=theme-color]').content` = `#09090B` while `body` background is `oklch(0.987 0 0)`. Website in light → `#FBFBFB`. Web Interface Guidelines, Dark Mode & Theming: "`<meta name="theme-color">` matches page background". |
| SH-11 | P3 | Scrollbar gutter | `apps/website/src/routes/__root.tsx:140` — `<html … className="scrollbar-gutter-stable">`. | `apps/web/src/routes/__root.tsx:167` — no such class and no `scrollbar-gutter` rule in `apps/web/src/styles/app.css`. On classic-scrollbar platforms every dialog/sheet open and every navigation between a scrolling and a non-scrolling page shifts the layout horizontally. Not observable on this macOS profile (overlay scrollbars). | `grep -n "scrollbar-gutter" apps/web/src` → no hits. Web Interface Guidelines, Safe Areas & Layout: "Use `scrollbar-gutter: stable` to prevent layout shift on modal open". |
| SH-12 | P3 | Header, ≤ `md` (round-1 carryover, unchanged) | `apps/website/src/components/layout/navbar.tsx:87,104` — System status and Search user stay in the header bar at every width. | `apps/web/src/components/layout/app-nav.tsx:72` gates `SystemStatus` with `max-md:hidden` and `apps/web/src/components/layout/nav-search.tsx:220` hides the search trigger below `md`; both are reachable only inside the mobile sheet (`mobile-nav.tsx:65-76,91`). | At 390 the web header is Open Menu / logo / Changelog / Open Menu; the website header is Toggle Menu / logo / System status / Changelog / Search user / Selected artist / Open Menu. |
| SH-13 | P3 | Artist scope in the nav (round-1 carryover, unchanged) | `apps/website/src/components/layout/navbar.tsx:106` — `SelectedArtistFilter` is a dedicated always-visible nav control. | Moved into the Settings dialog (`apps/web/src/features/settings/settings-dialog.tsx:144`) and the avatar submenu (`apps/web/src/components/layout/user-menu.tsx:103`). One click deeper, and signed out it is only reachable through the gear. | Compare the header inventories above. Recorded so it is not re-raised. |
| SH-14 | P3 | `/link/connect` title | `apps/website/src/routes/(container)/link/connect.tsx:16` — `m.page_titles_my_cosmo_link()` → `My Cosmo Link · Objekt Tracker`. | `apps/web/src/routes/(container)/link/connect.tsx:14` — `m.link_link_cosmo()` → `Link Cosmo · Objekt Tracker`. Every other route's title matches. | `curl` each and read `<title>`. |
| SH-15 | P3 | Grid image loading | `apps/website/src/components/objekt/objekt-view.tsx:85-91` — explicit `width={582} height={900}`, plus `fetchPriority={isPriority ? "high" : "auto"}`; callers mark the first three rows (`components/home/index-view.tsx:69`, `market/index-view.tsx:69`, `profile/profile-objekt.tsx:155`, `list/list-view.tsx:195` — all `rowIndex < 3`). | `apps/web/src/features/objekt/objekt-card.tsx:122-127` — **no `fetchPriority` anywhere in the app** and no intrinsic `width`/`height`, and callers mark only the first two rows (`routes/(container)/index.tsx:54`, `features/market/market-view.tsx:76`, `features/list/list-view.tsx:130` — `rowIndex < 2`). The LCP candidate loses its priority hint. | `grep -rn "fetchPriority" apps/web/src` → no hits. Measured in-browser: website's first-row cards report `loading:"eager", fetchpriority:"high"`; web's report `loading:"eager", fetchpriority:null`. Web Interface Guidelines, Images. |
| SH-16 | P3 | Bundle splitting | — | `apps/web/src/routes/(container)/list/$slug.tsx:55` exports `ListNotFound`, imported by `apps/web/src/routes/(container)/@{$nickname}_/list.$slug.tsx:15`. The router warns on every page load: *"These exports from …/list/$slug.tsx will not be code-split and will increase your bundle size: ListNotFound"*. The only console warning either app emits. | Load any route on `:3200` with the console open. Website emits nothing on any route. |
| SH-17 | P3 | Message catalogue | 702 keys, all of them reachable. | `apps/web/messages/en.json` carries 823 keys of which **117 have no `m.*` reader**, only three of them the sanctioned exemptions (`nav_my_cosmo_link`, `filter_selected_artist_aria`, `objekt_event_transferred`). The dead keys are the fingerprints of the gaps other reviewers own (`list_trade_matches_*` ×9, `list_trade_info_*` ×13, `filter_sort_by_*_desc` ×11, `objekt_menu_*`, `progress_*`) plus this area's `profile_not_found`, `nav_toggle_menu`, `nav_no_cosmo_found`, `nav_manage_cosmo_link`, `not_found_live`, `page_titles_market`, `page_titles_activity`. | Script in the report's method: parse `apps/web/messages/en.json`, subtract every `m.<key>` occurrence under `apps/web/src` outside `paraglide/`. |
| SH-18 | P3 | System status popover | `apps/website/src/components/layout/status-popover.tsx:36` — `format(date, "yyyy/MM/dd h:mm:ss a")`, e.g. `Last transfer: 2026/09/22 2:16:49 PM`. | `apps/web/src/components/layout/system-status.tsx:109` — `<TimeAgo>` renders `44 seconds ago`, with the absolute stamp only in the tooltip/`title` (`apps/web/src/components/shared/time-ago.tsx:16`). Everything else in the popover is identical. | Open the status popover on both; the two rows, badges and the partial-Cosmo hint match word for word. |
| SH-19 | P3 | Settings → Wide layout | `apps/website/src/components/layout/settings-modal.tsx:34,98` — the wide switch is hidden below a 1560 px viewport (`isCompact`). | `apps/web/src/features/settings/settings-dialog.tsx:128-134` renders it unconditionally, so a 390 px phone offers "Use full width for the collection grid", which does nothing it can perceive. | Open Settings at 390 on web; the switch is there. |
| SH-20 | P3 | ⌘K combobox | `apps/website/src/components/layout/user-search.tsx:71` — `autoFocus="first"` and React Aria keeps `aria-activedescendant` on the input, so the highlighted row is announced from the first keystroke (measured: `react-aria…-_r_13a_`). | `apps/web/src/components/layout/nav-search.tsx:242` sets `autoHighlight`, which highlights the first row visually, but the input's `aria-activedescendant` stays `null` until the user presses an arrow key — so a screen-reader user hears nothing about the row Enter would open. | Open ⌘K on web, type `kimmonday`, read the input's `aria-activedescendant` → `null`; press ArrowDown → `base-ui-_r_p7_-0`. `better-accessibility` → "Accessible names everywhere" / APG combobox. |
| SH-21 | P3 | Site-wide meta description | `home_description` = `"Cosmo objekt explorer"` (ko: `"Cosmo 오브젝트 탐색기"`), emitted on every route from `apps/website/src/routes/__root.tsx:53`. | `apps/web/messages/en.json` rewrites it to `"Browse every Cosmo objekt, filter by member, season and class, and see who holds each serial."` (`apps/web/src/routes/__root.tsx:70`). Better copy, but it is the description search engines already have indexed for every page — worth being a decision rather than a side effect. | `curl -s http://localhost:{3000,3200}/ \| grep 'name="description"'`. |
| SH-22 | P3 | ko/ja catalogue | — | Three web-only keys are still the English string in a non-English catalogue: `settings_artists` ("Artists") in `ja`, `compare_modal_source_id` ("ID: {id}") in `ko` and `ja`. Every other untranslated key in web is untranslated in the website too (proper nouns, placeholders, language names). | Compare `apps/web/messages/{ko,ja}.json` against `en.json` for identical values, then subtract the website's own identical set. |
| SH-23 | P3 | Skip link (both apps) | `apps/website/src/routes/__root.tsx:127` — one `<main>`, no skip link. | `apps/web/src/components/layout/page-main.tsx:21` — one `<main>`, no skip link either. Parity, but web makes it hurt more: the round-1 measurement of 50+ member chips in the natural tab order is unchanged, and web's header now carries 9 focusable controls before content against the website's 7. | `document.querySelectorAll('a[href^="#"]')` → empty on both. Both `better-accessibility` ("Structure is navigation") and the Web Interface Guidelines require a skip-to-content link ahead of repeated chrome. |
| SH-24 | P3 | Search trigger name | `apps/website/src/components/layout/user-search.tsx:57` — `aria-label={m.nav_search_user_label()}` → "Search user". | `apps/web/src/components/layout/nav-search.tsx:217-225` — the trigger has no `aria-label`, so its name is its text content plus the `<Kbd>`: **"Search user⌘K"**. Harmless but noisier, and the two apps name the same control differently. | `[...document.querySelectorAll('header button')].map(b => b.getAttribute('aria-label') ?? b.textContent)`. |
| SH-25 | P3 | Content gutter | `apps/website/src/components/intentui/container.tsx:12` — `--container-padding: --spacing(4)` (16 px). | `apps/web/src/components/layout/page-main.tsx:23` — `px-5` (20 px), and the nav uses the same (`app-nav.tsx:40`). Consistent within web, 4 px wider than the website at every breakpoint. | Measure `main`'s box at 1280: web content starts 20 px in, website 16 px. |
| SH-26 | P3 | Wide mode scope | `apps/website/src/components/shared/dynamic-container.tsx:9` applies `wide` to the page container only; the navbar's own `<Container>` (`navbar.tsx:76`) stays capped at the 2xl breakpoint, so in wide mode the nav no longer lines up with the content. | `apps/web/src/lib/utils.ts:16` puts the cap on a shared `containerClass` released by `[[data-wide]_&]`, which the nav uses too (`app-nav.tsx:40`) — so in wide mode the whole shell goes full-bleed. Measured at 1920: web nav inner box 187→0 px / 1536→1909 px together with `main`. Web is the coherent behaviour; recording the change. | Set `wide: true` in `web:settings`, reload at 1920, measure `header > div` and `main`. |

---

## Verified equal

- **Route surface.** `find apps/{website,web}/src/routes -type f` returns the same 37 paths, name for name, including `m/e.ts`, `m/s.ts` and all thirteen `api/*` handlers. Round 1's analytics gap is closed.
- **API handlers.** `api/activity.ts`, `api/auth.$.ts`, `api/collection.ts`, `api/objekts/list.$collectionSlug.ts`, `api/objekts/metadata.$collectionSlug.ts`, `api/objekts/transfers.$collectionSlug.$serial.ts`, `api/transfers.$address.ts`, `m/e.ts`, `m/s.ts` are **byte-identical**. `api/healthcheck.ts`, `api/live-sessions.ts`, `api/objekts/owned-by.$address.ts`, `api/open-app.ts`, `api/user.search.ts` and `rpc.$.ts` differ only in comment wording, destructuring and arrow-body style — same rate limits (30), same schemas, same status codes.
- **Server layer.** Both apps import the same `@repo/api`, so every ORPC router, service and env variable is literally shared; `packages/api/src/env.ts` is the single server schema and `apps/{website,web}/src/lib/env/client.ts` are **identical**. `lib/api-messages.ts`, `lib/auth-client.ts` and `lib/functions/profile.ts` are identical too; `lib/functions/{list,live}.ts` differ only in style. `server.ts` and `dev-websocket.ts` are identical apart from SH-2's port. Website's `requireAuth` middleware (`apps/website/src/lib/server/middleware.ts:5`) has no consumer in either app, so its absence from web is not a gap.
- **`public/`.** Byte-for-byte identical, all 21 files (favicons ×3, `apple-touch-icon.png`, two android-chrome icons, `site.webmanifest`, 13 `assets/icon-*.webp`). `/site.webmanifest` serves identically from both. Neither app ships `robots.txt` or `sitemap.xml` (404 on both).
- **SSR status codes.** `/`, `/market`, `/activity`, `/login`, `/terms-privacy`, `/live`, `/nonexistent` (404), `/@kimmonday` + `/trades` `/progress` `/stats` `/list`, `/@doesnotexistuser123` (404), `/list/zzz` (404), `/@kimmonday/list/zzz` (404), `/api/healthcheck` — all identical. `/list` and `/link` both 307 (different target, see below).
- **Redirects.** Trailing slashes (`/market/`, `/activity/`, `/@kimmonday/`) 307 to the bare path on both; `/live/<bad id>` 307s to `/live` on both; uppercase `/@KIMMONDAY` is 200 on both. `grep -rn "redirect(" apps/website/src` yields no legacy path web is missing.
- **Response headers.** `Vary: Origin`, `content-type`, chunked transfer identical on `/`; `/m/s` returns `cache-control: public, max-age=86400` and `application/javascript; charset=utf-8` from both. No `set-cookie` and no `content-language` from either.
- **Head tags.** Same `charSet`, `viewport`, `application-name`, 17 `keywords`, `referrer`, `format-detection`, `mobile-web-app-capable`, `apple-mobile-web-app-*`, manifest link, three `rel=icon`, `apple-touch-icon`, four `preconnect` hosts and the Space Grotesk `preload`. Only `color-scheme` differs — website `dark`, web `light dark` (web is correct, it has a light theme). Neither app emits `og:*`/`twitter:*`/canonical/robots on any route except `/live/$id`, where the two `head()` blocks produce the same title, description and image set.
- **Titles.** Every route's `<title>` resolves to the same string on both except SH-14, and the strings behind the differently-named keys are equal (`market_title` == `page_titles_market` == "Marketplace"; `activity_title` == `page_titles_activity` == "Activity"). Web adds titles the website lacks on `/auth/verified`, `/auth/reset-password` and `/@nickname/list`.
- **`<html lang>` and locale.** `PARAGLIDE_LOCALE=en|ko|ja` yields `lang="en"|"ko"|"ja"` from both, with no cookie falling back to `en`. Both use the same cookie name and `strategy: ["cookie", "baseLocale"]` (`vite.config.ts:51-52`). Shell strings render in the cookie's language on both (`오브젝트 / 마켓 / 활동 / 사용자 검색 / 로그인`, `Objekt / マーケットプレイス / アクティビティ / ユーザーを検索 / ログイン`).
- **Message coverage.** All 702 website keys exist in `apps/web/messages/{en,ko,ja}.json`; no key is missing from any locale; the ko and ja files have the same key count as `en`.
- **Fallback surfaces inside the container.** `/nonexistent`, `/@unknown`, `/list/zzz` and `/@nick/list/zzz` all render with the header and exactly one `<main>` on both apps (`apps/web/src/components/router/{not-found,error-boundary,pending}.tsx` each wrap in `PageMain`, whose `InPageMain` context prevents a nested second `<main>` — `page-main.tsx:16-17`). Web adds a way out ("Home", "Back to my lists") the website lacks.
- **Console and network.** Zero console errors and zero hydration warnings on `/`, `/market`, `/activity`, `/login`, `/terms-privacy`, `/live`, `/nonexistent`, `/@kimmonday` + four tabs, `/auth/verified`, `/auth/reset-password` on both apps at 1280. Zero 4xx/5xx sub-resource requests on either app on any of those routes (`performance.getEntriesByType('resource')`). Web's only console output is SH-16.
- **Theme before first paint.** Both avoid a flash: web with an inline `<head>` script (`apps/web/src/routes/__root.tsx:37-39,169`), website with tanstack-theme-kit's script at the top of `<body>`. Web additionally applies `wide` pre-paint (`data-wide` in the same script), which the website cannot — its `wide` only lands after hydration.
- **Analytics.** `/m/s` (cached script proxy) and `/m/e` (event proxy that rewrites the client IP) are byte-identical files and behave identically live (`/m/s` 200 + cache header, `/m/e` POST `{}` → 400 on both). `__root.tsx`'s script tag is gated identically on `import.meta.env.DEV || !clientEnv.VITE_UMAMI_WEBSITE_ID`.
- **Nav active state.** `/`, `/market`, `/activity` each mark exactly the right link on both; web's exact-match on `/` works (`app-nav.tsx:64`), and the mobile sheet closes on navigation (`mobile-nav.tsx:83`).
- **Mobile nav sheet.** Contains Search user (⌘K), Objekts, Marketplace, Activity, My List, System status and a Close — every desktop affordance is reachable at 390. Opens from the hamburger, closes on Escape, and **focus returns to the trigger**.
- **Dialog behaviour (web).** Changelog and Settings both carry an `aria-labelledby` title ("Changelog", "Settings") and an `aria-describedby`, move focus inside on open, mark the sibling roots hidden/inert, close on Escape and **restore focus to the exact trigger**. The changelog's `<details>` rows open the newest entry by default and are keyboard-operable natively.
- **Changelog content.** The nine dated entries in `apps/web/src/components/layout/changelog.tsx:24-87` are verbatim-identical to `apps/website/src/components/layout/changelog.tsx:20-83`, and the Discord invite note matches.
- **About dialog.** Same title, same `about_description` + `about_discord_invite` body, same GitHub and Discord links (`apps/web/src/components/layout/about.tsx` vs `apps/website/src/components/layout/about.tsx`). Web additionally reaches it from Settings, so signed-out visitors can open it.
- **⌘K quick search.** Opens on ⌘K/Ctrl+K, debounced server query against `/api/user/search`, results grouped, Recent capped at 7 with the same de-dupe rule (`search-store.ts:5,19-22` vs `use-user-search-store.ts:11,17-23`), "Clear history" as the last Recent row, Enter navigates to `/@{nickname}` and the pick lands in Recent (verified: `web:recent-users` gained `kimmonday` after Enter). Arrow keys move, Escape closes and restores focus. Web adds the `0x…` raw-address escape hatch ("Open address 0xdeadbeef") and a keyboard-hint footer the website has no equivalent of.
- **System status.** Same two rows (Database / Cosmo API), same badge wording (`Up to date`, `Partially down`), same partial-Cosmo hint sentence, same overall-status derivation (`if database.behind || cosmo === "down" → down; partial → partial; else up`), same skeleton shape. Only the timestamp format differs (SH-18).
- **Account dialog tab strip (round-1 HIGH, now fixed).** At 390×600 the `role="tablist"` stays at y=137 while the panel scrolls to the bottom (previously it left the dialog at y=−69). Four tabs: General, Social Link, Change Password, Danger zone.
- **Settings dialog content.** Theme, Language, Currency, Wide layout, Hide label — and round 1's missing `common_settings_theme_desc` / `common_settings_language_desc` are restored (`settings-dialog.tsx:77-79,99-101`). Web adds the Artists section and an About button.
- **Reflow.** `document.scrollWidth === clientWidth` at 390 on `/`, `/market`, `/@kimmonday`, `/login` on both apps in **en, ko and ja**. Web's root `overflow-x-clip` wrapper (`__root.tsx:155`) plus the dev overflow guard hold under the longest Korean and Japanese strings.
- **Reduced motion.** Web's global guard (`apps/web/src/styles/app.css:295-303`) covers `*`, `::before` and `::after` for animation duration, iteration count, transition duration and scroll behaviour — a strict superset of the website's single `.t-icon-swap` rule (`apps/website/src/styles/app.css:369-373`).
- **Invalid search params.** Web 307s to the canonicalised URL dropping only the invalid key (`/?member=SeoYeon&season=Atom01&sort=bogus&class=First` → `/?class=First&member=SeoYeon&season=Atom01`); website 200s and ignores them. Neither errors. Pasting a website URL into web and a web URL into website (`?member=`, `?sort=`, `?priced_only=`, `?floor_min=`, `?at=`, `?redirect=`, unknown keys) is 200/200 in every case tried.
- **Client bundle.** Web is **smaller**: 3,090,097 bytes of client JS across 89 chunks vs the website's 3,615,614 across 85. Web's biggest chunk is the lazily-routed Stream player (`player-BIbJDQ7a.js`, 575 KB, loaded only on `/live/$id` via `lazyRouteComponent`); the website ships the equivalent as `_id-BgfDYFS9.js` (593 KB). `apps/web/dist` was built today at 13:53 from this HEAD; `apps/website/dist` is from 2026-09-21 22:27, one day older — the website figure is indicative, not current. No build was run during this review.
- **Lint.** `apps/web` carries its own `oxlint.config.ts` extending `@repo/lint`, same as the website.

---

## Deliberate differences confirmed

- **Plan §5.3 — messages copied and pruned into `apps/web/messages`.** Confirmed: 823 keys against the website's 702, zero website keys missing, a separate `project.inlang` per app. The pruning half never happened (SH-17), but the copy decision itself is as designed.
- **Plan §5.2 — `packages/api` as the shared server layer.** Confirmed: both apps import `@repo/api` for every router, service and the server env schema; no query is re-implemented in `apps/web`.
- **Plan §1 — `apps/web` on port 3200 in dev** so lab (3100) and website (3000) keep running. Confirmed in `vite.config.ts:11`, `package.json` `scan`, and `server.ts:75` — but the production default inherits the dev port, which is SH-2.
- **Plan §1 — no `nuqs`, no `react-aria-components`, Base UI + Phosphor.** Confirmed for the shell: `lucide-react` appears only inside `apps/web/src/components/ui/**` (10 files), the verbatim cnippet registry `AGENTS.md` exempts; every icon in `components/layout/**` is Phosphor.
- **`openspec/specs/web-app-shell/spec.md` — "Theme is applied before first paint" and "Wide layout … applied before first paint".** Confirmed: the inline script at `__root.tsx:37-39` sets both `.dark` and `data-wide`; `useApplySettings` (`stores/settings.ts:53-70`) keeps them in sync and follows the OS under `System`. The website only does the theme half.
- **`openspec/specs/web-app-shell/spec.md` — "Locale comes from the locale cookie with English fallback".** Confirmed for `en`/`ko`/`ja` and for no cookie.
- **`openspec/specs/web-app-shell/spec.md` — "Unknown paths and failures render the shell's fallback surfaces … Both SHALL offer a way back to the home page".** Confirmed, and it is why web replaced the website's profile-specific not-found — which is exactly how SH-5 got in.
- **`openspec/specs/web-shell/spec.md` — "links to Objekts (`/`), Market, Activity and Lists".** Confirmed. The website's desktop nav has only Marketplace and Activity, so Objekts and My List are deliberate additions. Round 1's "My List shown while signed out" is fixed — `app-nav.tsx:23` filters it out without a session, which is a small drift from the spec's "on every page" but the better behaviour.
- **`openspec/specs/web-shell/spec.md` — "a query that starts with `0x` and matches no user SHALL offer to open it as a raw address"** and the Recent/Clear-history contract. Confirmed live; the website has no raw-address escape hatch at all.
- **Round 1 P3 — `/list` and `/link` signed out redirect to `/login?redirect=<href>`** rather than the website's `/`. Confirmed still in place (`location: /login?redirect=%2Flist` vs `location: /`); web is the better behaviour.
- **Round 1 P3 — artist scope moved out of the nav** (SH-13) and **System status / search moved into the mobile sheet** (SH-12). Both unchanged; re-listed as gaps only because they remain visible differences, not because they are regressions.

---

## Could not verify

- **A running signed-in `apps/website`.** `VITE_SITE_URL=http://localhost:3200` pins Better Auth's `baseURL` (`packages/api/src/services/auth.ts`) to the web origin, so the `better-auth.session_token` cookie returns `null` on `:3000`. SH-7 and SH-8 are therefore source-only comparisons against `apps/website/src/components/layout/user-nav.tsx`; the web side of both was exercised live.
- **The website's Wide toggle at runtime.** Two attempts to flip `config.wide` through `localStorage` were overwritten by the store's own `onRehydrateStorage` write-back (`use-config.ts:37-39`), so SH-26's website half is read from `dynamic-container.tsx:9` and `navbar.tsx:76`, not measured. The web half was measured (187→0 px, 1536→1909 px at 1920).
- **Production container behaviour.** SH-1 means no `web` image can be built, so nothing about the runtime image, its healthcheck, its secrets or its `EXPOSE` was testable. SH-2's restart-loop is inferred from `server.ts:75` against `docker-compose.yml:94,128`, not observed.
- **A fresh production build.** Per the brief, no build was run. The bundle comparison uses the existing `apps/web/dist` (current HEAD) and `apps/website/dist` (one day older), so the 3.09 MB / 3.62 MB split is directional.
- **Classic-scrollbar layout shift (SH-11).** macOS overlay scrollbars make the gutter 0 px in this browser; the regression is inferred from the missing class and the guideline, not seen.
- **Forced-colors mode and a real screen reader.** Focus indicators, names, roles and states were read from the DOM and the computed accessibility properties; no NVDA/VoiceOver pass and no `forced-colors` emulation was run.
- **`VITE_ACTIVITY_WEBSOCKET_URL` in production.** It is declared in both client env schemas (`lib/env/client.ts:8`) but is passed neither as a Docker build secret (`apps/website/Dockerfile:17-20`) nor in the compose environment (`docker-compose.yml:104-126`). Pre-existing on the website side and therefore not a parity gap, but C8 should decide whether the live activity feed is meant to be on.

---

## C8 cutover checklist (input for the cutover change)

1. Author `apps/web/Dockerfile` (SH-1) — copy `apps/website/Dockerfile`, swap `website`→`web` in the prune target, the `build:prod` filter and the `dist` copy, and keep the four `VITE_*` build secrets.
2. Fix the listening port (SH-2): either `PORT=3000` in the compose environment or change `apps/web/server.ts:75`'s default. `EXPOSE 3000/tcp` and the `/api/healthcheck` curl must agree with it.
3. Add `apps/web/turbo.json` (SH-3).
4. `docker-compose.yml:91-97` — rename the `website` service and repoint `dockerfile:` at `./apps/web/Dockerfile`.
5. `.github/workflows/docker-ci.yml:45` (`apps/website/**` → `apps/web/**`) and `:114` (matrix `dockerfile`). The matrix key is already called `web`, so the image name `…-web` is unchanged.
6. `knip.json` — add an ignore for `apps/web/src/components/ui/**` before the gate goes live (SH-6).
7. Decide on a one-time `localStorage` migration, or accept that every user's device settings reset (SH-4).
8. `AGENTS.md:11,20,48,50,66,99,122` and `README.md:34,82` still name `apps/website`.
9. `.superset/config.json` `run` still starts `--filter=lab` alongside `--filter=web`; lab is deleted at C8.
