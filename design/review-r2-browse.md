# Parity review round 2 — browse

Date: 2026-09-22
Commits reviewed: `157b09c7` (HEAD on `web/main`), plus the fix worker's uncommitted edits to
`apps/web/src/features/{market/market-view.tsx,objekt/drawer/index.tsx,objekt/drawer/serials.tsx}`
that landed mid-review (drawer default tab + serials loading region — both out of scope, read as current state).
Reference: `apps/website` at the same commit.

| | |
| --- | --- |
| Viewports | 1280×900 and 390×844 via `Emulation.setDeviceMetricsOverride` |
| Themes | dark and light (web's theme is the `web:settings` store, not `prefers-color-scheme`; toggled via the store and restored afterwards) |
| Browser | `browser-use` CLI, two owned CDP targets (`Target.createTarget`), every read after an explicit `switch_tab` |
| Skills invoked | `better-interface` (→ `better-accessibility`, `better-layout`, `better-writing`, `better-typography`, `better-colors`, `better-ui`), `code-review`, `router-core`, `start-core` |
| Docs read first | `AGENTS.md`, `design/web-review.md`, `design/web-migration-plan.md` §5, `openspec/specs/web-{objekt-browser,market,activity,lists,compare}/spec.md`, `git log 340b1222..HEAD -- apps/web` |

**Signed-in surfaces were compared live, not from source.** Contrary to round 1's caveat, the
browser profile resolves the `better-auth.session_token` cookie on **both** origins in this session:
`POST http://localhost:3000/rpc/user/currentUser` returns the smoke account (`C7 Smoke`). Every
signed-in surface below (`/list`, list detail owner actions, select bar, add-to-list) was therefore
opened on both apps. No mutation was submitted anywhere; write flows were read from source only.

**Locale note.** Part-way through the run another agent set `PARAGLIDE_LOCALE=ja` on the shared
browser; it was reset to `en` and every count/copy claim below was taken with `document.documentElement.lang === "en"`.

## Gaps

| ID | Sev | Surface | Website does (file:line) | Web does (file:line) | Repro |
| --- | --- | --- | --- | --- | --- |
| BR-1 | **P1** | every filtering surface (`/`, `/market`, `/activity`, `/list/$slug`, `/@n/list/$slug`) | Array parameters are **comma-joined into one key**: nuqs `parseAsArrayOf` with its default `,` separator — `apps/website/src/hooks/use-filters.ts:44-58`. Picking two seasons writes `?season=Atom01,Binary01` (observed). | `list()` wraps a lone string into a one-element array and never splits on `,` — `apps/web/src/features/filters/search-schema.ts:18-25`; `stringifySearch` emits **repeated keys** — `apps/web/src/features/filters/search-params.ts:28-46`. Picking two seasons writes `?season=Atom01&season=Binary01` (observed). | Every multi-value link from the live site opens a different view. `/?member=YooYeon,SeoYeon&season=Atom01` → website **165 total**, web **0 total**. `/?class=Double,Special` → **8,309** vs **0**. `/?artist=tripleS,artms` → **12,980** vs **15,548** (scope silently dropped). `/?edition=1,2` → web strips the parameter from the URL entirely (`location.search === ""`). Reverse direction: `/?member=YooYeon&member=SeoYeon&season=Atom01` → web 165, website **96** (nuqs reads the first value only). Affects `member`, `artist`, `season`, `class`, `collection`, `on_offline`, `edition`. |
| BR-2 | **P1** | `/list/$slug`, `/@n/list/$slug` (incl. compare results) | List entries and compare results are tagged before filtering — `apps/website/src/hooks/use-list-objekt.ts:24` and `:39` (`select: (data) => data.map(mapObjektWithTag)`), which fills `tags` and `edition`. | `listEntriesOptions` — `apps/web/src/features/list/queries.ts:19-20` — and `useCompareQuery` — `apps/web/src/features/compare/use-compare.ts:35-52` — never call `mapObjektWithTag`, so entries reach `filterObjekts` with no `tags` and no `edition`. Search reads `objekt.tags` at `apps/web/src/features/filters/filter-utils.ts:87`. | Search and the Edition facet are dead on every list. `/@kimmonday/list/dsddd?search=hayeon` → website 3 cards, web **0**. `?edition=1` → 3 vs **0**. `?search=%23300-400` → 3 vs **0**. The same search on `/` works in web (`/?search=hayeon` → 381 total), so it is list-scoped. |
| BR-3 | **P1** | `/`, `/market` | Rarity is an offered sort on both — `apps/website/src/lib/utils.ts:47` (`defaultSort`) and `:48-57` (`marketSort`) — and the rarity map is fetched whenever `sort === "rare"` — `apps/website/src/hooks/use-collection-objekt.ts:16`, `use-market-objekts.ts:23`. | `HOME_SORTS` — `apps/web/src/features/filters/filter-bar.tsx:67` — and `MARKET_SORTS` — `apps/web/src/features/market/market-view.tsx:32-40` — omit `rare`, and neither `use-collection-objekts.ts` nor `use-market-objekts.ts` builds a rarity map. `sortObjekts` then bails: `if (!rarityMap) return []` — `apps/web/src/features/filters/filter-utils.ts:213`. | Sort by Rarity is unreachable, and a `sort=rare` link renders a blank page that claims it is full. `/?sort=rare` → website 45 card images; web **0 images**, `document.documentElement.scrollHeight === 1021`, while the count line above the empty grid still reads **"15,548 total"** and the Sort trigger reads "Sort by: Rarity". |
| BR-4 | **P1** | `/list/$slug` header (have / want lists, owner) | **Trade Matches**: a button in the list header — `apps/website/src/components/list/list-header.tsx:85` — opening a modal with a mode selector (Have→Want / Both / Want→Have), a per-partner disclosure list off `tradePartnersQuery`, and an info popover explaining the three modes — `apps/website/src/components/list/trade/trade-matches.tsx`, `trade-matches-content.tsx`. | Not ported. `apps/web/src/features/list/list-header.tsx:78-101` has no such control; `apps/web/src/features/list/queries.ts` has no trade-partner query; **24 `list_trade_*` keys sit in `apps/web/messages/en.json` with zero readers** (`grep -rn "list_trade_" apps/web/src` → nothing, only `components/layout/changelog.tsx` prose). | Sign in as a list owner, open a Have or Want list on both. Website shows "Trade Matches" beside Edit; web shows Share / Compare / Export / Edit / Delete and no way to reach matches. |
| BR-5 | **P2** | `/list/$slug` sort menu | Sort options are list-aware — `apps/website/src/components/list/filter.tsx:34-37`: `defaultSortDuplicateSerial` (date, season, collectionNo, member, **serial**, **duplicate**, **rare**) for a profile-bound list without hidden serials, `defaultSortDuplicate` otherwise, **plus `price`** when the list type is `sale` (`apps/website/src/lib/utils.ts:58-77`). | `ListView` passes no `sorts`, so `FilterBar` falls back to `HOME_SORTS` — `apps/web/src/features/list/list-view.tsx:177-182`, `apps/web/src/features/filters/filter-bar.tsx:294`. Serial, Duplicate, Rarity and Price are absent from the menu; `?sort=rare` additionally empties the grid via BR-3's `return []`. | Open a sale list on both: website's Sort menu offers Price, web's offers only Date / Season / Collection No. / Member. |
| BR-6 | **P2** | objekt drawer on `/list/$slug` (profile-bound list) | A first **Owned** tab listing every copy the profile holds — serial, token id, received, transferable — with sortable Serial/Received columns, 10-per-page pagination and a per-row menu (pin, move pin up/down, lock, add to list): `apps/website/src/components/objekt/objekt-panel.tsx:65-90` and `:105-250`, switched on by `modalTab={list.isProfileBind && !list.hideSerial ? "owned" : "trades"}` — `apps/website/src/components/list/list-view.tsx:49`. | The drawer has three tabs only — `apps/web/src/features/objekt/drawer/index.tsx:212-214` (Serials / Market / Metadata). `objekt_owned`, `objekt_not_owned`, `objekt_owned_table_aria`, `objekt_pagination_next_aria` are all unread in `apps/web/src`. | Open a card on a profile-bound list in both. Website lands on Owned with the copy table; web lands on Serials and the table does not exist anywhere. |
| BR-7 | **P2** | create-list and edit-list dialogs | **Hide Serial Numbers** (sale/have lists, enabled once profile-bound) and **Hide User** are checkboxes on the form — `apps/website/src/components/list/modal/create-list-modal.tsx:383-417`, mirrored in `edit-list-modal.tsx:40-44,143-148`. | `ListDraft` carries both so an edit does not reset them, but `ListForm` renders no control for either — `apps/web/src/features/list/list-form.tsx:44-49` (the comment says so outright) and the form body at `:141-352`. `list_create_hide_serial_label/desc` and `list_create_hide_user_label/desc` are unread in `apps/web/src`. | Open Create list on both. Website shows the two checkboxes; web does not, so neither flag can be set or cleared any more. `hideSerial` also drives the list grid's serial display, so the setting is now write-once via the website. |
| BR-8 | **P2** | objekt drawer → Market tab | An infinite query, 20 rows a page, with the auto-loading sentinel — `apps/website/src/components/objekt/market-view.tsx:39-56` and `:96-101` (`InfiniteQueryNext`). | One request capped at 100 rows and no next page — `apps/web/src/features/objekt/queries.ts:57-61` (`offset: 0, limit: 100`) consumed by a plain `useQuery` at `apps/web/src/features/objekt/drawer/market.tsx:47`; nothing renders a pager. | Open the Market tab on a collection with more than 100 live listings (several of the `from $1.50` rows on `/market` have 3-figure listing counts): the website keeps loading, web stops at 100 with no indication that more exist. |
| BR-9 | **P2** | objekt drawer → Market tab, one listing row | Row shows the seller's **own** price and currency, with an approximate conversion when it differs from the viewer's — `apps/website/src/components/objekt/market-view.tsx:214-226` (`formatPrice(item.price, item.currency)` + `≈{formatUsd(item.usdPrice)}`) — and a **View list** link to the seller's sale list — `:246-256`. | Only the converted figure is shown — `apps/web/src/features/objekt/drawer/market.tsx:178-184` (`formatUsd(item.usdPrice)`) — and there is no link to the listing's list. `objekt_market_view_list` is unread in `apps/web/src`. | Open Market on any listed collection: website reads e.g. `MYR 6.12 ≈$1.50` and offers View list; web reads `$1.50` with no route to the seller's list. |
| BR-10 | **P2** | `/@nickname/list` | Renders every list the endpoint returns — `apps/website/src/components/profile/profile-list.tsx:45`. The endpoint applies no bind filter: `packages/api/src/routers/list-entries.ts:38-57` → `fetchOwnedLists("profileAddress", …)` in `packages/api/src/services/list.ts:238-267`, which selects on `profileAddress` alone. | Filters the result down to bound lists — `apps/web/src/features/list/profile-lists.tsx:19` (`data.filter((list) => list.isProfileBind)`). A list filed under a Cosmo for display (`profileAddress` set, `isProfileBind` false — the case `m.list_create_display_profile_desc` describes) disappears from the tab. | Source-level; the smoke data has no display-only list, so this could not be reproduced at runtime (see "Could not verify"). |
| BR-11 | **P2** | `/list/$slug` card, sale list | The entry's **note** is a popover beside the price badge — `apps/website/src/components/objekt/objekt-view.tsx:120-133`, gated at `:64`. | `ObjektCard` has no note affordance at all — `apps/web/src/features/objekt/objekt-card.tsx:204-215` renders only the price string. The only note reader in web is the drawer's Market tab (`apps/web/src/features/objekt/drawer/market.tsx:185`). Yet Set price still writes it — `apps/web/src/features/list/set-price-dialog.tsx:69,144-149`. | Set a note on a sale-list entry via the website, then open that list in web: the note is invisible to anyone browsing the list, only visible to someone who finds the same listing through an objekt drawer. |
| BR-12 | **P2** | `/activity` | The feed pages itself as you scroll — `apps/website/src/components/activity/activity-render.tsx:372-377` → `components/shared/infinite-query-pending.tsx:22-35` (`InView`, `rootMargin 0 0 900px 0`), and shows an end-of-feed marker when there is nothing left. | A **Load more** button that must be clicked — `apps/web/src/features/activity/activity-view.tsx:332-343` — and no end-of-feed state. | Scroll to the bottom of `/activity` on both. Website keeps appending; web stops and waits for a click. |
| BR-13 | P3 | sort menu, every surface | Each sort option carries a one-line description — `apps/website/src/components/filters/filter-sort.tsx:63` (`MenuDescription`), fed by the eleven `filter_sort_by_*_desc` keys. | The select lists labels only — `apps/web/src/features/filters/filter-bar.tsx:128-132`. All eleven `filter_sort_by_*_desc` keys are unread in `apps/web/src`. | Open Sort by on `/` in both. |
| BR-14 | P3 | `/list/$slug` (where Duplicate/Serial are offered) | Picking a sort also adjusts Combine duplicates — `apps/website/src/components/filters/filter-sort.tsx:43` (`grouped: value === "duplicate" ? true : value === "serial" ? null : current.grouped`). | `SortSelect` writes `sort` and `sort_dir` only — `apps/web/src/features/filters/filter-bar.tsx:124`. | Moot today because BR-5 hides those two sorts; it becomes visible the moment BR-5 is fixed. |
| BR-15 | P3 | every surface, sort control | `sort_dir` is set to `asc` only for serial / member / rare / floor and cleared (→ `desc`) otherwise — `apps/website/src/components/filters/filter-sort.tsx:43`. | `SORT_DEFAULT_DIR` also makes season, collectionNo and price ascend — `apps/web/src/features/filters/filter-bar.tsx:52-64`. | Pick "Season" on `/` in both: website sorts newest season first, web oldest first. Pasted URLs still agree, because both default a missing `sort_dir` to `desc`. |
| BR-16 | P3 | objekt drawer, mint summary | Four figures, with the label following the objekt type: **Copies** / **Scanned copies**, Spin, **Non-Spin**, Tradable — `apps/website/src/components/objekt/objekt-attribute.tsx:53-66`. | Three figures, one label for both types: Minted / Spun / Transferable — `apps/web/src/features/objekt/drawer/serials.tsx:188-204`. `objekt_copies`, `objekt_scanned_copies`, `objekt_non_spin`, `objekt_tradable` are all unread in `apps/web/src`. | Same objekt (`Atom01 YooYeon 100Z`): website "Copies 3,817 · Spin 0 · **Non-Spin 3,817** · Transferable 0.00% (0)"; web "Minted 3,817 · Spun 0 · Transferable 0.00% (0)". Observed on a physical objekt (`Atom01 YooYeon 120A`, Type Physical): web reads "Minted 87" where the website reads "Scanned copies 87". |
| BR-17 | P3 | objekt drawer, attributes and Metadata | A unit objekt lists **every** member — `apps/website/src/components/objekt/objekt-attribute.tsx:83-85` (`(objekt.members.length > 0 ? objekt.members : [objekt.member]).map(...)`). | One `Member` row from `objekt.member` — `apps/web/src/features/objekt/drawer/index.tsx:122` and `apps/web/src/features/objekt/drawer/metadata.tsx:55`. | Open any `id7 X id11` unit objekt (visible on `/activity`): website shows a pill per member, web shows the combined id string only. |
| BR-18 | P3 | objekt drawer → Metadata | Edition renders as an ordinal — `apps/website/src/components/objekt/objekt-attribute.tsx:88` (`getEditionStr` → "1st"/"2nd"/"3rd", `apps/website/src/lib/utils.ts:141-143`). | Raw integer — `apps/web/src/features/objekt/drawer/metadata.tsx:62` (`objekt.edition ?? "—"`) — even though web already has the ordinal map at `apps/web/src/features/filters/labels.ts:10`. | Open any objekt with an edition; Metadata reads `Edition 1`. |
| BR-19 | P3 | `/market`, `/list/$slug` counts | `ObjektCount` always prints both halves of the localised count and adds a "· N types" figure when Combine duplicates is on — `apps/website/src/components/collection/objekt-count.tsx:24-40`, used with `grouped` at `apps/website/src/components/list/list-view.tsx:271`. | `/` prints the prefix (`apps/web/src/routes/(container)/index.tsx:74`) but `/market` and list detail print the suffix only — `apps/web/src/features/market/market-view.tsx:104-106`, `apps/web/src/features/list/list-view.tsx:226-228` — and no surface prints the types count. `common_count_types_prefix/suffix` are unread in `apps/web/src`. | Switch the locale to `ko`: `/` reads "총 15,548개" and `/market` reads "9,709개". Turn on Combine duplicates on a list: the "· N types" figure never appears. |
| BR-20 | P3 | `/market` floor-price popover | The two number fields are currency-formatted — `apps/website/src/components/market/filter-floor-price.tsx:32,42` (`formatOptions: { style: "currency", currency }`), so the input reads `$5.00`. | Plain numbers — `apps/web/src/features/market/filter-floor-price.tsx:43-71` passes no `format`. | Open Floor price on `/market` in both with a value set. |
| BR-21 | P3 | search field, every surface | An explicit clear button replaces the help `?` once there is text — `apps/website/src/components/filters/filter-search.tsx:69-78`. | Relies on the native `type="search"` clear, which Firefox does not draw — `apps/web/src/features/filters/filter-search.tsx:105-118`. `filter_search_clear_aria` is unread in `apps/web/src`. | Type into Quick search in Firefox: no clear control. |
| BR-22 | P3 | objekt drawer timeline, drawer Market rows, `/activity` Time column | Relative times go through `Intl.RelativeTimeFormat(getLocale())` — `apps/website/src/lib/utils.ts:33-43` — and the trade table prints absolute, sortable timestamps — `apps/website/src/components/objekt/trade-view.tsx:295,321`. | `relativeTime` hard-codes the English `" ago"` suffix and `absoluteTime` a fixed `d MMM yyyy, HH:mm` — `apps/web/src/lib/time.ts:7-17` — and `TimeAgo` is the only renderer (`apps/web/src/components/shared/time-ago.tsx:11-22`). The drawer timeline also has no date sort. | Set the locale to `ko` or `ja` and open `/activity`: every Time cell still reads "5 seconds ago". (Flagging for the i18n reviewer; the surfaces are mine.) |
| BR-23 | P3 | `/`, `/market`, `/list/$slug` selection | A persistent row of controls — Select mode, **Select all**, Add to list, Clear select — is portalled under the filters whenever signed in, before anything is picked — `apps/website/src/components/home/index-view.tsx:105-112`, `components/filters/select-mode.tsx:17-40`. | `SelectBar` returns `null` until at least one card is selected — `apps/web/src/features/objekt/select-bar.tsx:44` — so "Select all" is reachable only after a first long-press or hover-check. | Open `/` signed in on both: website shows Select all straight away, web shows nothing. |
| BR-24 | P3 | objekt drawer → Market tab, default sort | Default sort is **Date** except when the drawer was opened from `/market`, where it is Price — `apps/website/src/components/objekt/objekt-panel.tsx:96` (`defaultSortBy={initialTab === "market" ? "price" : "createdAt"}`), `components/objekt/market-view.tsx:27-28`. | Always Price ascending — `apps/web/src/features/objekt/drawer/market.tsx:45-46`. | Open the Market tab from `/` on both. (Distinct from the drawer's *default tab*, which the fix worker owns.) |
| BR-25 | P3 | `/list/$slug` card, Combine duplicates on | Serial is hidden for the whole grid while grouping is on — `apps/website/src/components/list/list-view.tsx:187` (`showSerial={!filters.grouped && …}`). | Serial is hidden per card, only when that card stands for more than one token — `apps/web/src/features/objekt/objekt-card.tsx:85` (`hideSerial || (qty !== undefined && qty > 1)`). | Turn on Combine duplicates on a bound list: singleton cards keep their serial in web and lose it on the website. |
| BR-26 | P3 | `/list/$slug` card price | The price badge is the Set price control for the owner, and reads "Set price" when none is set — `apps/website/src/components/objekt/objekt-view.tsx:58-63,105-118`. | The price is inert text; Set price lives only in the card menu and the select bar — `apps/web/src/features/objekt/objekt-card.tsx:204-215`, `apps/web/src/features/list/list-view.tsx:134-139`. | Own a sale list, open it on both, click an unpriced card's price. |
| BR-27 | P3 | `/activity` paused-on-hover notice | The notice shows for as long as the pointer is over the table, whether or not anything is queued — `apps/website/src/components/activity/activity-render.tsx:362-369`. | It shows only once a row has actually been withheld — `apps/web/src/features/activity/activity-view.tsx:346-355`. | Hover a quiet feed on both. Web is arguably better; recorded so it is not re-raised. |

## Verified equal

- **Grid data and ordering, `/`.** `15,548 total` on both with no filters; `?member=YooYeon&season=Atom01` → 96 on both, with the same card sequence (A120A, A119A, A118A, A117A, A338Z, A337Z, A334Z, A335Z, A336Z, A219Z, A218Z, A217Z, A216Z, A330Z …) despite different column counts.
- **Single-value URL round-trip.** `?member=`, `?season=`, `?class=`, `?collection=`, `?on_offline=`, `?edition=`, `?search=`, `?color=`+`?colorSensitivity=`, `?sort=`+`?sort_dir=`, `?group_by=`+`?group_dir=`, `?transferable=`, `?grouped=`, `?hidePin=`, `?locked=`, `?priced=`, `?unowned=`, `?missing=`, `?floor_min=`/`?floor_max=`, `?at=` — a 24-parameter URL is preserved byte-for-byte by web's `validateSearch` and re-emitted unchanged. `?color=%23FF0000&colorSensitivity=12` → 335 on both.
- **Reset.** The desktop Reset filter button is back on every surface (`apps/web/src/features/filters/filter-bar.tsx:236-256`, `:340`), is disabled when nothing is set, and preserves `at` exactly as `useResetFilters` does on the website (`apps/web/src/features/filters/use-filters.ts:11,68-71` vs `apps/website/src/hooks/use-reset-filters.ts`). Round 1's P1 is fixed.
- **Filter predicate.** `filterObjekts` is line-for-line equivalent — member/artist/class/season/on_offline/edition/locked/priced/floor range, comma-OR + space-AND + `!` negation grammar, `#n`/`#n-m` serial ranges, `a201z-aa204z` collection ranges, chroma ΔE colour matching with the same default sensitivity of 7 (`apps/web/src/features/filters/filter-utils.ts:96-191` vs `apps/website/src/lib/filter-utils.ts:88-172`). Web adds case-insensitive member matching, which fixes a real website bug (`?member=Yooyeon` returns 0 on the website and 599 in web).
- **`/market`.** Identical totals — `9,709 total · 52,168 listings` on both; identical first rows and floor prices (`from $1.50`, `$18.00`, `$5.00`, `$1.00` = `MYR 6.12`, `73.42`, `20.40` at the website's MYR setting); QYOP / "Ask" fallbacks match (`apps/web/src/features/market/price-label.ts:9-18` vs `apps/website/src/components/market/index-view.tsx:48-53`); listing-count pill in the same corner; default sort `listedAt` on both.
- **`/activity` data and filters.** Event type filter (`?type=spin` → 56 vs 58 rows live), Type (digital/physical) filter (`?on_offline=offline` → 42 vs 45 rows live) — round 1's missing control is back as a single-select with an "All" item matching the website's `FilterOnline`. Artist / Member / Season / Class / Collection No. all present, Reset clears facets *and* the event type in one navigation.
- **Socket feed.** Same live-transfer semantics: client-side re-filter of pushed rows, dedupe against page 0, hold-while-hovering then flush, one-shot highlight (`apps/web/src/features/activity/activity-view.tsx:110-232` vs `apps/website/src/components/activity/activity-render.tsx:196-268`).
- **Objekt drawer, data.** `Atom01 YooYeon 100Z` resolves identically: Artist tripleS (the round-1 "triples" bug is fixed), Member YooYeon, Season Atom01, Class Welcome, Collection No. 100Z, Type Digital, Owner HarryPotter, Token ID 5, Transferable No, mint 2022/08/01, Transferable share `0.00% (0)`. Serial stepping logic (first/prev/next/last snapping to existing serials) is character-identical to `apps/website/src/components/objekt/trade-view.tsx:93-116`.
- **Drawer states.** idle / loading / error / private / missing / found all present and reached by the same conditions (`apps/web/src/features/objekt/drawer/serials.tsx:230-252` vs `apps/website/src/components/objekt/trade-view.tsx:212-233`).
- **Drawer keyboard.** Esc closes and focus returns to the originating card (measured: `document.activeElement` is the card's `role="button"` wrapper afterwards). Flip card is a single `<button>` with `aria-pressed` and `aria-label`, so Enter/Space work as they do on the website's `role="button"` div.
- **Drawer Market stats.** Floor / Listings / Sellers bar agrees with the website's, same three labels, same shimmer-on-load behaviour.
- **Compare.** `cmp_type` / `cmp_to` / `cmp_mode` round-trip unchanged in both directions. Error state matches word for word — `?cmp_to=doesnotexist123` gives "Target profile not found" + Cancel on both. Success matches — `?cmp_type=profile&cmp_to=kimmonday&cmp_mode=missing` gives 18 entries and the same banner ("Showing Objekts missing from target / Target: kimmonday (Profile)") on both; `cmp_mode=matches` gives 0 on both. The compare dialog's fields are the same plain text inputs on both sides (`apps/website/src/components/compare/modal/{profile,list}-selector.tsx` are `TextField`s, not pickers).
- **Add to list dialog.** Same list-availability rule (bound lists only when the surface supplies a matching profile address), same default `skipDups = true`, same "no lists yet → create one here" note (`apps/web/src/features/list/add-to-list-dialog.tsx:96-104` vs `apps/website/src/components/list/modal/add-to-list-modal.tsx:96-105`). Nothing was submitted.
- **Set price dialog.** Same fields (QYOP toggle, price, note ≤255 chars), same clear semantics; web additionally exposes the Clear button the website renders `hidden`.
- **Export.** Same dialog copy, same `list.export` call and blob download (`apps/web/src/features/list/export-button.tsx` vs `apps/website/src/components/list/modal/export-list-modal.tsx`).
- **Discord format (visitor, filtered set).** Same five flags, same Group by (None / Season / Season first), same Style (Default / Compact) disabled while Group by is None, same `## Have` header and identical `format()` output path; web adds a Reset button. Present on list detail for any visitor — round 1's P1 is fixed.
- **Collection No. facet.** Web's combobox is a superset of the website's autocomplete (search, count, clear) with the same option list.
- **Filter popover long tail.** The inert switches round 1 found are gone: `LONG_TAIL` now names the website's matrix per surface (`apps/web/src/features/filters/filter-popover.tsx:52-60`), and Type is a single-select radio group with "All", matching `FilterOnline`.
- **Search help.** The `?` popover with all eleven operator lines plus intro and example is back (`apps/web/src/features/filters/filter-search.tsx:27-57`), as is ⌘F/Ctrl+F focus (`:84-98`). Both round-1 gaps fixed.
- **Member chip strip.** Round 1's two HIGH accessibility findings are fixed: the strip is one tab stop with roving `tabindex` and arrow/Home/End navigation (`apps/web/src/features/filters/member-chips.tsx:22-57`), and hidden content now has prev/next buttons alongside the fade (`:62-121`).
- **Reflow.** `document.scrollWidth === clientWidth === 390` on `/`, `/market`, `/activity`, `/@kimmonday/list/dsddd` and with the drawer open at 390×844, in both themes. No page-level horizontal scroll anywhere in scope.
- **Umami.** `apps/web/src/routes/m/s.ts` and `m/e.ts` are byte-identical to the website's (`diff` empty), and the script tag in `__root.tsx:120-129` matches `apps/website/src/routes/__root.tsx:103-112` exactly (same `src`, `data-host-url`, `data-website-id`, same dev/no-id guard). Neither app calls `umami.track()` anywhere, so only automatic pageviews fire and they fire identically.
- **List index `/list`.** Same create and Discord-format entry points, same card grid, same edit/delete menu; web adds an empty state.
- **List detail header.** Name, Verified badge, list-type badge, `(CUR)` for sale lists, owning-Cosmo link, owner avatar + Discord/Twitter badges, Swap to Have/Want, Edit, Delete — all present; web adds Share.
- **Lock filter tri-state.** `locked` absent → `true` → `false` → absent, matching the spec and `apps/website/src/components/filters/filter-locked.tsx`.
- **Contrast.** No failing pair found in either theme on `/`, `/market`, `/activity` or list detail at either viewport; round 1's measured table still holds.

## Deliberate differences confirmed

- **`/activity` rows are not stacked below `md`.** Web keeps one 640px grid in an `overflow-x-auto` box (`apps/web/src/features/activity/activity-table.tsx:10-11`); measured 640/348 at 390px. The website has three layouts. The user chose to keep web's version — recorded, not filed.
- **Objekt drawer default tab on `/market`** and the **drawer empty states** — being changed by a concurrent fix worker; excluded per the brief. (Their uncommitted edit to `market-view.tsx:133` and `drawer/index.tsx` was in the tree during this review.)
- **Plan §5.5 — compare deviation.** The default was "match website, do not hide list edits while comparing". Web does exactly that, with the reasoning in a comment: `apps/web/src/features/list/list-view.tsx:242-244` keeps Remove and Set price on the select bar while a comparison is on screen, and only the card menu's write items are gated (`:137-149`).
- **Metadata tab as a superset.** Accent/Text colour and Created at moved off the attribute list into Metadata, which also adds Slug, Collection ID, Edition, Minted at and the four image URLs. Round 1 recorded this as intentional.
- **Collection No. offered on every surface** (web) vs `/activity` and `/@n/trades` only (website), applied client-side with the divergence documented at `apps/web/src/features/filters/filter-utils.ts:126-128`. Round 1 P3, still true, still a superset.
- **Type facet lives in the Filters popover** in web rather than inline. Round 1 P3.
- **Card identity.** Web's caption is `Member` + short collection no. (`YooYeon A120A`) and the band carries the same values; the website's badge is `getCollectionShortId`. Verified visually identical in content at 1280.
- **Activity objekt cell** shows member + short no. + thumbnail in web vs the full `collectionId` in the website — the lab's card language, applied consistently.

## Could not verify

- **BR-10 at runtime.** The smoke data has no list with `profileAddress` set and `isProfileBind` false, and creating one is a mutation. The finding rests on `fetchOwnedLists` (`packages/api/src/services/list.ts:242-267`) selecting on `profileAddress` alone while `apps/web/src/features/list/profile-lists.tsx:19` filters on `isProfileBind`.
- **BR-8 at runtime.** No collection in the current market data was confirmed to carry more than 100 live listings within the review window; the 100-row cap is from source (`apps/web/src/features/objekt/queries.ts:58`).
- **Every mutation flow** — add to list, remove from list, set price, create/edit/delete list, pin, lock, list export submission. Dialogs were opened and read; nothing was submitted, per the brief and the production-`.env` rule. Their parity above is a source comparison only.
- **Sale-list price sorting end to end (BR-5).** The control is absent in web, so the underlying `sort=price` path could not be exercised through the UI; `?sort=price` is accepted by the schema but the option never appears in the menu.
- **`hideUser` effect on the list header.** The flag has no control in web (BR-7) and no list in the data sets it, so the header's behaviour under it was not observed on either app.
