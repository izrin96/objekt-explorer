{
  "$schema": "https://turborepo.dev/schema.json",
  "ui": "tui",
  "globalDependencies": ["packages/lint/oxlint.config.ts", ".oxfmtrc.json"],
  "tasks": {
    "dev": {
      "cache": false,
      "persistent": true
    },
    "build": {
      "dependsOn": ["^build"],
      "outputs": ["dist/**", ".next/**", "!.next/cache/**"]
    },
    "start": {
      "cache": false,
      "persistent": true
    },
    "lint": {
      "dependsOn": ["^lint"],
      "inputs": ["$TURBO_DEFAULT$", "oxlint.config.ts"],
      "outputs": [],
      "cache": true
    },
    "lint:fix": {
      "inputs": ["$TURBO_DEFAULT$", "oxlint.config.ts"],
      "outputs": ["**/*.{ts,tsx,js,jsx}"],
      "cache": false
    },
    "typecheck": {
      "dependsOn": ["^typecheck"],
      "inputs": ["$TURBO_DEFAULT$", "tsconfig.json"],
      "outputs": [],
      "cache": true
    },
    "format": {
      "inputs": ["$TURBO_DEFAULT$", ".oxfmtrc.json"],
      "outputs": ["**/*.{ts,tsx,js,jsx,json,css,md}"],
      "cache": false
    }
  }
}
