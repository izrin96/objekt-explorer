{
  "$schema": "https://turborepo.dev/schema.json",
  "extends": ["//"],
  "tasks": {
    "build": {
      "outputs": ["lib/**"]
    },
    "lint": {
      "inputs": ["$TURBO_DEFAULT$", "oxlint.config.mts"]
    }
  }
}
